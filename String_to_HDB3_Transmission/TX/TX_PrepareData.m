% Function:     Set Message Data as HDB3 and adds parity
% Parameters:   Data - Input binary secuence
%               Data - Output binary secuence HDB3 with parity
% Coder:        A.Vargas
function dataOut = TX_PrepareData(dataVector)
    % separar el vector en bites
    for(index = 1:1:length(dataVector))
        
        
    end
    % quitar bit sobrante
    % Aplicar paridad
    % regenerar señal
end

